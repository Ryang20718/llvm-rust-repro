#[cfg(test)]
mod tests {
    #[test]
    fn test_main_runs() {
        log::info!(
            "Worker {} extracting polylines for map {}, processing {} geometries of {} total geometries",
            1,
            2,
        );
    }
}
